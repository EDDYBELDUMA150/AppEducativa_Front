<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/lorenzo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lorenzo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppEducativa"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".controlador.SelTipoAprendizaje"
            android:exported="false" />
        <activity
            android:name=".controlador.InicioApp"
            android:exported="false" />
        <activity
            android:name=".controlador.Registro"
            android:exported="false" />
        <activity
            android:name=".controlador.MainSesion"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>